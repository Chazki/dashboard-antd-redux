import React from "react";
import { Typography } from "antd";
const { Title } = Typography;

const RegisterFormTitle = () => {
  return <Title level={2}>Regístrate</Title>;
};

export default RegisterFormTitle;
